# ===== DEVELOPMENT DOCKERFILE FOR NEXT.JS WEB APP =====
FROM node:18-alpine
WORKDIR /app

COPY package.json package-lock.json* ./
RUN npm install

EXPOSE 3000

# Next.js development server runs on start
CMD ["npm", "run", "dev"]
